const press_add = document.getElementById("add");
const input_add = document.getElementById("add_input");
const list_add = document.getElementsByClassName("list");
const feedback = document.getElementById("feedback)");


function buttonpressed(){
    list_add=[]
    if (input_add.value == ""){
        feedback.innerText= "You have an empty input"}
        else {list_add.value}
    }
        
